version: '3.8'

services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  # ComfyUI (선택 - 로컬에서 별도 실행 가능)
  # comfyui:
  #   image: comfyui/comfyui:latest
  #   ports:
  #     - "8188:8188"
  #   volumes:
  #     - ./comfyui_models:/app/models
  #     - ./comfyui_input:/app/input
  #     - ./comfyui_output:/app/output
  #   environment:
  #     - NVIDIA_VISIBLE_DEVICES=all
  #   deploy:
  #     resources:
  #       reservations:
  #         devices:
  #           - driver: nvidia
  #             count: 1
  #             capabilities: [gpu]

volumes:
  redis_data:
